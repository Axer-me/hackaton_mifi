# ── Core: обучение и инференс ─────────────────────────────────────────────────
# Шаг 1 — torch/torchvision (CPU):
#   pip install torch==2.9.1 torchvision==0.24.1 --index-url https://download.pytorch.org/whl/cpu
# Шаг 2 — остальные пакеты:
#   pip install -r requirements.txt
#
# GPU (CUDA 11.8) — заменить шаг 1:
#   pip install torch==2.9.1 torchvision==0.24.1 --index-url https://download.pytorch.org/whl/cu118
# GPU (CUDA 12.8) — если ставите готовые +cu128 wheels с pytorch.org или локально:
#   pip install torch==2.9.1 torchvision==0.24.1 --index-url https://download.pytorch.org/whl/cu128
torch==2.9.1
torchvision==0.24.1
timm==1.0.22

# ── Данные и аугментации ──────────────────────────────────────────────────────
albumentations==2.0.8
albucore==0.0.24
opencv-python==4.10.0.84
Pillow==11.3.0
numpy==2.3.4
pandas==2.3.3

# ── Метрики и ML ──────────────────────────────────────────────────────────────
# sklearn: train-метрики, StratifiedKFold (run_convnext_3fold), stacking (scripts/stacking_convnext_3fold.py)
scikit-learn==1.7.2

# ── Визуализация ──────────────────────────────────────────────────────────────
matplotlib==3.10.7
seaborn==0.13.2

# ── Утилиты ───────────────────────────────────────────────────────────────────
tqdm==4.67.1
PyYAML==6.0.3
requests==2.32.3

# ── Jupyter ───────────────────────────────────────────────────────────────────
ipykernel==7.1.0
ipython==9.6.0

# ── LLM-фильтрация эвристик (Шаг 2б) ─────────────────────────────────────────
anthropic==0.49.0

# ── Прототип интерфейса (опционально) ─────────────────────────────────────────
# gradio
# streamlit
